# Converting Decimal number to binary number using recursion
def DecToBin(n):
    if n > 1:
        DecToBin(n // 2)
    print(n % 2, end='')


# decimal number
dec = int(input('Enter any decimal number: '))

DecToBin(dec)
print()
